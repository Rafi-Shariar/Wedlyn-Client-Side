import React from 'react';
import { Button } from "@/components/ui/button"
const Test = () => {
    return (
        <div>
            <Button variant="default">Button</Button>
        </div>
    );
};

export default Test;